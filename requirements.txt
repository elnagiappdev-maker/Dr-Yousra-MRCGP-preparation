# Pinned for reproducibility and environment control
streamlit==1.36.0
pandas==2.2.2
requests==2.32.3

# Storage Options (Choose/uncomment as needed)
supabase==2.5.0
psycopg2-binary==2.9.9 # Postgres driver for Supabase
gspread==6.1.1
google-auth==2.29.0
google-cloud-firestore==2.17.0

# Authentication
streamlit-authenticator==0.2.2

# Development & CI tools (installed via Actions, not strictly needed for Streamlit Cloud runtime)
pydantic==2.7.3 # For clean config management
black==24.4.2
ruff==0.4.9
mypy==1.10.0
pytest==8.2.2
